/**
 * Dropship store-identity primitives, rebuilt on the unified design system.
 * The Catalyst kit itself is imported per-file (`@/components/ui/button`…),
 * not through this barrel.
 */
export { StoreLogo } from './StoreLogo';
export { StoreAvatar } from './StoreAvatar';
